import { useState } from 'react'; import Layout from '../../components/Layout'; import ToolActions from '../../components/ToolActions';
const lorem='Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.';const secure=(length,chars)=>{const values=new Uint32Array(length);crypto.getRandomValues(values);return[...values].map(n=>chars[n%chars.length]).join('')};
export default function RandomGenerator(){const[mode,setMode]=useState('Random string');const[length,setLength]=useState(32);const[output,setOutput]=useState('');const generate=()=>{if(mode==='Lorem ipsum')setOutput(Array.from({length:Math.max(1,Math.min(20,length))},()=>lorem).join('\n\n'));else setOutput(secure(Math.max(1,Math.min(4096,length)),'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%^&*_-'))};return <Layout title="Lorem & Random Generator" description="Generate placeholder paragraphs or cryptographically secure random strings locally."><div className="flex flex-wrap gap-3"><select className="textarea-base max-w-52" value={mode} onChange={e=>setMode(e.target.value)}><option>Random string</option><option>Lorem ipsum</option></select><div><label className="sr-only" htmlFor="random-length">{mode==='Lorem ipsum'?'Paragraphs':'Length'}</label><input id="random-length" type="number" min="1" max={mode==='Lorem ipsum'?20:4096} className="textarea-base max-w-32" value={length} onChange={e=>setLength(Number(e.target.value))}/></div><button className="btn" onClick={generate}>Generate</button></div><textarea aria-label="Generated output" className="textarea-base h-72 mt-4 font-mono" value={output} readOnly/><ToolActions value={output} onClear={()=>setOutput('')} filename="generated.txt"/></Layout>}
