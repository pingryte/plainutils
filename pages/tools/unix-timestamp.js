import { useEffect,useState } from 'react'; import Layout from '../../components/Layout'; import ToolActions from '../../components/ToolActions';
const describe=(date,utc)=>({iso:date.toISOString(),display:utc?date.toUTCString():date.toLocaleString(),relative:new Intl.RelativeTimeFormat(undefined,{numeric:'auto'}).format(Math.round((date-Date.now())/86400000),'day')});
export default function UnixTimestamp(){const[timestamp,setTimestamp]=useState('');const[dateInput,setDateInput]=useState('');const[result,setResult]=useState(null);const[error,setError]=useState('');const[utc,setUtc]=useState(false);const[now,setNow]=useState(Math.floor(Date.now()/1000));useEffect(()=>{const id=setInterval(()=>setNow(Math.floor(Date.now()/1000)),1000);return()=>clearInterval(id)},[]);const fromEpoch=()=>{const n=Number(timestamp);const date=new Date(n*(String(Math.trunc(Math.abs(n))).length>10?1:1000));if(!Number.isFinite(n)||Number.isNaN(date.getTime())){setError('Enter a valid epoch timestamp.');return}setResult(describe(date,utc));setError('')};const fromDate=()=>{const date=new Date(dateInput);if(Number.isNaN(date.getTime())){setError('Enter a valid date, preferably ISO 8601.');return}setTimestamp(String(Math.floor(date.getTime()/1000)));setResult(describe(date,utc));setError('')};return <Layout title="Unix Timestamp Converter" description="Convert epoch seconds or milliseconds and ISO dates with UTC, local, and relative output."><div className="panel mb-5 flex justify-between"><span>Current epoch</span><button className="font-mono text-blue-600" onClick={()=>setTimestamp(String(now))}>{now}</button></div><div className="grid md:grid-cols-2 gap-5"><div><label className="field-label" htmlFor="epoch">Epoch seconds or milliseconds</label><input id="epoch" className="textarea-base" value={timestamp} onChange={e=>setTimestamp(e.target.value)}/><button className="btn mt-2" onClick={fromEpoch}>Convert epoch</button></div><div><label className="field-label" htmlFor="date">Date or ISO 8601</label><input id="date" className="textarea-base" value={dateInput} onChange={e=>setDateInput(e.target.value)} placeholder="2026-07-18T12:00:00Z"/><button className="btn mt-2" onClick={fromDate}>Convert date</button></div></div><label className="block mt-4"><input type="checkbox" checked={utc} onChange={e=>setUtc(e.target.checked)}/> Prefer UTC display</label>{error&&<p className="error-message" role="alert">{error}</p>}{result&&<div className="panel mt-5 space-y-2"><p><strong>ISO:</strong> <code>{result.iso}</code></p><p><strong>{utc?'UTC':'Local'}:</strong> {result.display}</p><p><strong>Relative:</strong> {result.relative}</p></div>}<ToolActions value={result?.iso||''} onClear={()=>{setTimestamp('');setDateInput('');setResult(null)}} filename="timestamp.txt"/></Layout>}
