import { useEffect, useState } from 'react'; import { ArrowDown, Play, Plus, Trash2 } from 'lucide-react'; import Layout from '../components/Layout'; import FileDrop from '../components/FileDrop'; import ToolActions from '../components/ToolActions'; import { applyTransform, pipelineTransforms } from '../lib/transforms';
const newStep=()=>({id:crypto.randomUUID(),transform:'json-format'});
export default function Workspace(){const[input,setInput]=useState('');const[steps,setSteps]=useState([]);const[results,setResults]=useState([]);const[error,setError]=useState('');const[running,setRunning]=useState(false);useEffect(()=>{const saved=localStorage.getItem('plainutils:pipeline-input');if(saved){setInput(saved);localStorage.removeItem('plainutils:pipeline-input')}const savedSteps=localStorage.getItem('plainutils:pipeline-steps');if(savedSteps)try{setSteps(JSON.parse(savedSteps))}catch{}},[]);useEffect(()=>{if(steps.length)localStorage.setItem('plainutils:pipeline-steps',JSON.stringify(steps))},[steps]);const run=async()=>{setRunning(true);setError('');const next=[];let value=input;try{for(const step of steps){value=await applyTransform(step.transform,value);next.push(value)}setResults(next)}catch(e){setError(`Step ${next.length+1}: ${e.message}`);setResults(next)}finally{setRunning(false)}};const output=results.at(-1)||input;return <Layout title="Pipeline Workspace" metaTitle="Local Tool Pipeline Workspace | PlainUtils" description="Chain multiple PlainUtils transformations into a reusable local workflow."><div className="grid lg:grid-cols-[1fr_22rem] gap-6"><section><FileDrop accept=".txt,.json,.yaml,.yml,.csv,.md,.html,.css,.js" onFile={({text})=>setInput(text)}/><label className="field-label mt-4" htmlFor="pipeline-input">Pipeline input</label><textarea id="pipeline-input" className="textarea-base h-52 font-mono" value={input} onChange={e=>setInput(e.target.value)}/><ToolActions value={output} onClear={()=>{setInput('');setResults([])}} filename="pipeline-output.txt"/><label className="field-label mt-5" htmlFor="pipeline-output">Final output</label><textarea id="pipeline-output" className="textarea-base h-64 font-mono" value={output} readOnly/></section><aside><div className="flex justify-between items-center mb-3"><h2 className="text-xl font-bold">Steps</h2><button className="btn-secondary" onClick={()=>setSteps([...steps,newStep()])}><Plus/> Add</button></div>{!steps.length&&<p className="panel text-sm text-gray-500">Add transformations, then run the pipeline. Steps are saved on this device.</p>}<div className="space-y-2">{steps.map((step,index)=><div key={step.id}><div className="panel flex gap-2 items-center"><span className="font-bold">{index+1}</span><select aria-label={`Transformation ${index+1}`} className="textarea-base !p-2" value={step.transform} onChange={e=>setSteps(steps.map(item=>item.id===step.id?{...item,transform:e.target.value}:item))}>{pipelineTransforms.map(([value,label])=><option value={value} key={value}>{label}</option>)}</select><button className="icon-button" aria-label={`Remove step ${index+1}`} onClick={()=>setSteps(steps.filter(item=>item.id!==step.id))}><Trash2/></button></div>{index<steps.length-1&&<ArrowDown className="w-4 h-4 mx-auto text-gray-400"/>}</div>)}</div><button className="btn w-full mt-4" onClick={run} disabled={running||!steps.length}><Play/>{running?'Running…':'Run pipeline'}</button>{error&&<p className="error-message" role="alert">{error}</p>}</aside></div></Layout>}
